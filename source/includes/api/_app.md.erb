App API
====================
All API endpoints for App integration. Only clients with `scope` containing `app` can access these API endpoints.


<%= partial "api/app/agency" %>
<%= partial "api/app/ad-group" %>
<%= partial "api/app/causes" %>
<%= partial "api/app/clients" %>
<%= partial "api/app/uploads" %>

